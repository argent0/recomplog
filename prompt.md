In `recomplog report html`

# Cards

The values shown should be:

Weight (already present)
Body Fat % (already present)
Muscle % (missing)
Muscle mass (kg) (missing)

Remove the other cards.

## Card values to show

The `label` (already present)

the N (from --days N) median (instead of the last value)

The N days trend (present)

The last recorded value

